import { slugify } from "../cli/utils.js";
export function getPresetByName(presets, requested) {
    const presetsByName = new Map(Array.from(presets.map((preset) => [slugify(preset.about.name), preset])));
    return (presetsByName.get(slugify(requested)) ??
        new Error(`${requested} is not one of: ${Array.from(presetsByName.keys()).join(", ")}`));
}
//# sourceMappingURL=getPresetByName.js.map