import { CreatedConfig } from "../../config/types.js";
export interface MigrationSource {
    descriptor: string;
    load: () => Promise<CreatedConfig | Error | symbol>;
    type: "config file" | "template";
}
export interface RequestedMigrationSource {
    configFile?: string;
    directory: string;
    from?: string;
    requestedPreset?: string;
    yes?: boolean;
}
export declare function parseMigrationSource({ configFile, directory, from, requestedPreset, yes, }: RequestedMigrationSource): Error | MigrationSource;
