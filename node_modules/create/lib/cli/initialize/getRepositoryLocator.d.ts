export interface RepositoryLocator {
    owner: string;
    repository: string;
}
export declare function getRepositoryLocator(options: Record<string, unknown>): {
    owner: string;
    repository: string;
};
