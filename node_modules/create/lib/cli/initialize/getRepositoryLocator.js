export function getRepositoryLocator(options) {
    if (!isStringLike(options.owner)) {
        throw new Error(`To run with --mode initialize, --owner must be a string-like, not ${typeof options.owner}.`);
    }
    if (!isStringLike(options.repository)) {
        throw new Error(`To run with --mode initialize, --repository must be a string-like, not ${typeof options.repository}.`);
    }
    return {
        owner: options.owner.toString(),
        repository: options.repository.toString(),
    };
}
function isStringLike(value) {
    return ["boolean", "number", "string"].includes(typeof value);
}
//# sourceMappingURL=getRepositoryLocator.js.map