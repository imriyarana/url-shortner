import { BlockModifications } from "../config/types.js";
import { AnyShape, InferredObject } from "../options.js";
import { CreatedBlockAddons, Creation } from "../types/creations.js";
import { ProductionMode } from "../types/modes.js";
import { Preset } from "../types/presets.js";
import { NativeSystem } from "../types/system.js";
import { Template } from "../types/templates.js";
export interface ProduceTemplateSettings<OptionsShape extends AnyShape> extends Partial<NativeSystem> {
    addons?: CreatedBlockAddons<any, InferredObject<OptionsShape>>[];
    blocks?: BlockModifications<InferredObject<OptionsShape>>;
    directory?: string;
    mode?: ProductionMode;
    offline?: boolean;
    options: InferredObject<OptionsShape>;
    preset: Preset<OptionsShape> | string;
}
export declare function produceTemplate<OptionsShape extends AnyShape>(template: Template<OptionsShape>, { addons, blocks: blockModifications, directory, mode, offline, options, preset: requestedPreset, ...providedSystem }: ProduceTemplateSettings<OptionsShape>): Promise<Creation<InferredObject<OptionsShape>>>;
