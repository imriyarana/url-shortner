import { Imported } from './types.js';

interface ImportLocalOrNpxSettings {
    confirm?: NpxConfirm;
    importer?: ModuleImporter;
    logger?: NpxImportLogger;
}
type ModuleImporter = (source: string) => Promise<object>;
type NpxConfirm = (specifier: string) => Promise<Error | undefined>;
type NpxImportLogger = (message: string) => void;
declare function importLocalOrNpx(specifier: string, { confirm, importer, logger, }?: ImportLocalOrNpxSettings): Promise<Imported>;

export { type ImportLocalOrNpxSettings, type ModuleImporter, type NpxConfirm, type NpxImportLogger, importLocalOrNpx };
