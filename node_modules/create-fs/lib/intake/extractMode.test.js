import { describe, expect, test } from "vitest";
import { extractMode } from "./extractMode.js";
describe("extractMode", () => {
    test.each([
        [parseInt("0x81ed", 16), "wat"],
        //
    ])("%i becomes %i", (raw, expected) => {
        expect(extractMode(raw)).toBe(expected);
    });
});
//# sourceMappingURL=extractMode.test.js.map