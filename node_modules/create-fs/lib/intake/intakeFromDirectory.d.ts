import { CreatedDirectory } from "../types/files.js";
export interface IntakeFromDirectorySettings {
    exclude?: RegExp;
}
export declare function intakeFromDirectory(directoryPath: string, settings?: IntakeFromDirectorySettings): Promise<CreatedDirectory>;
